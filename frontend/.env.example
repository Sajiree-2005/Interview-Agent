# Base URL of the FastAPI backend (interview-agent-backend).
# Locally this is usually http://localhost:8000
# In production, point this at your deployed Render/Railway/Fly URL.
VITE_API_URL=http://localhost:8000
